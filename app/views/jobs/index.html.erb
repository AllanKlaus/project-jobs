<div class="row">
  <aside class="col-sm-4">
    <h2>Side Menu</h2>
  </aside>
  <section class="col-sm-8">
    <h1>Recent Jobs</h1>
    <% if !@jobs.empty? %>
        <%= render @jobs %>
    <% else %>
        <%= controller_empty('jobs') %>
    <% end %>
  </section>
</div>
